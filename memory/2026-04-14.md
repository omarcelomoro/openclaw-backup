# 2026-04-14

- Marcelo confirmou a direção de relatórios no padrão visual dark da Leevre, mas com entrega final obrigatória em PDF.
- Foram criados os guias `empresa/padrao-visual-relatorios-pdf-leevre.md` e `rotinas/geracao-relatorios-pdf-leevre.md` para fixar esse padrão.
- Foi criado o template base `state/template-relatorio-pipeline-leevre.html` e o script `state/gerar_pdf_relatorio.py` para converter HTML em PDF.
- Marcelo reclamou que os PDFs não chegaram no Telegram quando enviados só com referência `MEDIA:`; isso indica que o fluxo de entrega de arquivo precisa validação prática antes de prometer envio concluído.
- Foi criado o template `state/template-relatorio-leads-esfriando-leevre.html` como segundo relatório padrão da Leevre.
- Para o bloco operacional de comissão e conciliação, foram criadas duas skills autorais:
  - `skills/conciliacao-comissao-leevre/SKILL.md`
  - `skills/relatorio-comissao-conciliacao-pdf-leevre/SKILL.md`
- Recomendação consolidada para esse bloco: separar a skill de processo de conciliação da skill de geração do PDF executivo.
- Foi validada a primeira emissão real de NFSe da Leevre pelo portal/API da Virei Contador em modo assistido.
- A nota emitida foi para `PORTO SEGURO COMPANHIA DE SEGUROS GERAIS`, valor `R$ 16.002,71`, número `57`, status final `EMITIDA`.
- O payload correto do emissor foi confirmado pela API `/api/sales`, usando `clientId`, `items[].productId` e os campos fiscais extraídos do serviço da própria Leevre, antes de chamar `/api/sales/{id}/emitir-nfse`.
- Foi baixado o PDF da nota emitida em `state/nf-teste/NFSE-PORTO-SEGUROS-57.pdf`.
- Como a senha do portal foi enviada por chat, foi reforçada a recomendação de troca posterior dessa credencial.
- Depois da emissão validada, foi criado o registro técnico `state/nf-teste/primeira-emissao-real-2026-04-14.md` para documentar payload, endpoints e IDs usados no caso real.
- Também foi criado o script assistido `state/emitir_nf_virei_contador.py` para repetir o fluxo via API com menos risco, sem precisar remontar manualmente as requisições.
- A aba `lancamentos_mensais` da planilha de NF foi preenchida com os tomadores ativos vindos do `cadastro_base` para a competência `2026-04`, já deixando a linha da PORTO SEGUROS atualizada como `EMITIDA`, nota `57`.
- O modelo cancelado `PORTO SEGURO` não foi replicado na aba mensal, para evitar confusão com o caso real validado da `PORTO SEGUROS`.
- Foi criada a matriz operacional inicial de seguradoras em `areas/operacoes/matriz-operacional-seguradoras-valores.md`, com classificação por tipo de fluxo, prioridade e regra de credenciais para orientar a automação da captura dos valores mensais.
- Também foi criado o checklist reutilizável `areas/operacoes/checklist-mapeamento-seguradora.md` para mapear cada seguradora sem sair pedindo todas as credenciais de uma vez.
- A PORTO SEGUROS já ficou parcialmente mapeada em `areas/operacoes/mapeamento-porto-seguros.md`, com tela, filtros e regra de competência, mas o link profundo enviado pelo usuário foi tratado como sessionizado/sensível e não deve ser usado como URL permanente de automação.
- Depois o usuário corrigiu o caminho funcional da Porto para a tela `COMISSÕES - NOTA FISCAL ELETRÔNICA`, com filtros de `Mês` e `Filial` e botão `Gerar relatório`, o que melhorou bastante o mapeamento da captura do valor mensal.
- Com o print do relatório da Porto, ficou validado que o campo correto para preencher `valor_nota` é o `VALOR BRUTO`, que o relatório já traz um valor consolidado e que as retenções aparecem separadas (`ISS`, `IR`, `INSS`), sem necessidade de soma manual na tela principal.
- Foi criado o playbook operacional da Porto em `areas/operacoes/playbook-porto-seguros-captura-e-nota.md`, consolidando a captura do valor mensal e a regra de emissão da NFSe.
# 2026-04-14

- Marcelo confirmou a direção de relatórios no padrão visual dark da Leevre, mas com entrega final obrigatória em PDF.
- Foram criados os guias `empresa/padrao-visual-relatorios-pdf-leevre.md` e `rotinas/geracao-relatorios-pdf-leevre.md` para fixar esse padrão.
- Foi criado o template base `state/template-relatorio-pipeline-leevre.html` e o script `state/gerar_pdf_relatorio.py` para converter HTML em PDF.
- Marcelo reclamou que os PDFs não chegaram no Telegram quando enviados só com referência `MEDIA:`; isso indica que o fluxo de entrega de arquivo precisa validação prática antes de prometer envio concluído.
- Foi criado o template `state/template-relatorio-leads-esfriando-leevre.html` como segundo relatório padrão da Leevre.
- Para o bloco operacional de comissão e conciliação, foram criadas duas skills autorais:
  - `skills/conciliacao-comissao-leevre/SKILL.md`
  - `skills/relatorio-comissao-conciliacao-pdf-leevre/SKILL.md`
- Recomendação consolidada para esse bloco: separar a skill de processo de conciliação da skill de geração do PDF executivo.
- Foi validada a primeira emissão real de NFSe da Leevre pelo portal/API da Virei Contador em modo assistido.
- A nota emitida foi para `PORTO SEGURO COMPANHIA DE SEGUROS GERAIS`, valor `R$ 16.002,71`, número `57`, status final `EMITIDA`.
- O payload correto do emissor foi confirmado pela API `/api/sales`, usando `clientId`, `items[].productId` e os campos fiscais extraídos do serviço da própria Leevre, antes de chamar `/api/sales/{id}/emitir-nfse`.
- Foi baixado o PDF da nota emitida em `state/nf-teste/NFSE-PORTO-SEGUROS-57.pdf`.
- Como a senha do portal foi enviada por chat, foi reforçada a recomendação de troca posterior dessa credencial.
- Depois da emissão validada, foi criado o registro técnico `state/nf-teste/primeira-emissao-real-2026-04-14.md` para documentar payload, endpoints e IDs usados no caso real.
- Também foi criado o script assistido `state/emitir_nf_virei_contador.py` para repetir o fluxo via API com menos risco, sem precisar remontar manualmente as requisições.
- A aba `lancamentos_mensais` da planilha de NF foi preenchida com os tomadores ativos vindos do `cadastro_base` para a competência `2026-04`, já deixando a linha da PORTO SEGUROS atualizada como `EMITIDA`, nota `57`.
- O modelo cancelado `PORTO SEGURO` não foi replicado na aba mensal, para evitar confusão com o caso real validado da `PORTO SEGUROS`.
- Foi criada a matriz operacional inicial de seguradoras em `areas/operacoes/matriz-operacional-seguradoras-valores.md`, com classificação por tipo de fluxo, prioridade e regra de credenciais para orientar a automação da captura dos valores mensais.
- Também foi criado o checklist reutilizável `areas/operacoes/checklist-mapeamento-seguradora.md` para mapear cada seguradora sem sair pedindo todas as credenciais de uma vez.
- A PORTO SEGUROS já ficou parcialmente mapeada em `areas/operacoes/mapeamento-porto-seguros.md`, com tela, filtros e regra de competência, mas o link profundo enviado pelo usuário foi tratado como sessionizado/sensível e não deve ser usado como URL permanente de automação.
- Depois o usuário corrigiu o caminho funcional da Porto para a tela `COMISSÕES - NOTA FISCAL ELETRÔNICA`, com filtros de `Mês` e `Filial` e botão `Gerar relatório`, o que melhorou bastante o mapeamento da captura do valor mensal.
- Com o print do relatório da Porto, ficou validado que o campo correto para preencher `valor_nota` é o `VALOR BRUTO`, que o relatório já traz um valor consolidado e que as retenções aparecem separadas (`ISS`, `IR`, `INSS`), sem necessidade de soma manual na tela principal.
- Foi criado o playbook operacional da Porto em `areas/operacoes/playbook-porto-seguros-captura-e-nota.md`, consolidando a captura do valor mensal e a regra de emissão da NFSe.
- Marcelo perguntou se, ao enviar o caminho/origem de onde cada seguradora mostra os valores, a Livri conseguiria preencher isso automaticamente.
- Direção consolidada: sim, a automação deve evoluir para ler a origem informada por seguradora, preencher automaticamente os `valor_nota` na base mensal e depois emitir apenas as linhas marcadas com `emitir_agora = SIM`.
- Marcelo pediu para rodar apenas a PORTO SEGUROS com competência `02/2026` em fluxo completo.
- A credencial da Porto foi confirmada em `~/.openclaw/secrets/porto.env` e a da Virei Contador foi salva em `~/.openclaw/secrets/virei.env`.
- Foi tentada uma primeira emissão assistida da PORTO SEGUROS para competência `02/2026`, mas ela saiu com valor incorreto por não ter validado antes o relatório correto da Porto para fevereiro.
- O usuário enviou o print da Porto mostrando o relatório correto de `02/2026`, com `VALOR BRUTO = R$ 18.102,94`.
- A NF incorreta foi a `58`, venda `3014`, valor `R$ 16.002,71`, `spedyNfsId` `69cc1f21-445c-4709-9f12-412cfdec69fb`.
- Foi confirmado no bundle/frontend do portal que o cancelamento usa `POST /api/sales/{id}/cancelar-nfse` com payload `{reason}`.
- A NF `58` foi cancelada com sucesso via API, usando o motivo de valor incorreto para a competência `02/2026`.
- Depois foi feita a reemissão correta da PORTO SEGUROS para competência `02/2026` via `state/emitir_nf_virei_contador.py`.
- Dados da reemissão correta: venda `3019`, NFSe `59`, valor `R$ 18.102,94`, status final `EMITIDA`, `spedyNfsId` `ae90362a-40c3-443c-a476-eb525109f0c3`.
- O PDF correto ficou salvo em `state/nf-teste/NFSE-PORTO SEGURO COMPANHIA DE SEGUROS GERAIS-59.pdf`.
- A planilha `lancamentos_mensais` foi corrigida em `A17:P18`, deixando a NF `58` como `CANCELADA` e a NF `59` como emissão válida para a Porto em `02/2026`.
- Depois Marcelo informou que também cancelou a NF `59`, porque ela era uma NF de teste.
- Lição operacional consolidada: a skill/processo de emissão de nota fiscal está validado tecnicamente, mas em testes deve tratar a emissão como sensível e confirmar explicitamente se a nota deve permanecer válida ou ser cancelada ao fim do teste.
- Estado consolidado da Porto em `02/2026`: fluxo técnico de captura + emissão foi validado, mas as NFs `58` e `59` acabaram canceladas por contexto de teste, então não devem ser tratadas como notas válidas em aberto.
- Marcelo enviou o mapeamento inicial da BANESTES.
- A BANESTES ficou consolidada com portal `https://portalinternet.banestes.com.br/banseg/sala_corretor/restrito/comissao/menu_comissoes.jsf`, tela `Comissões - EXTRATO DE PAGAMENTOS` e regra de competência por período completo do mês anterior, por exemplo `01/03/2026` a `31/03/2026`.
- Também ficou registrada a janela operacional da BANESTES: as notas podem ser geradas até o 5º dia do mês subsequente.
- Foram criados `areas/operacoes/mapeamento-banestes.md` e `areas/operacoes/playbook-banestes-captura-e-nota.md`.
- O print da BANESTES validou que o campo correto para preencher `valor_nota` é o total de `Valor Base`, não `Valor Pago`.
- Exemplo validado por print: período `01/03/2026` a `31/03/2026`, `Valor Pago total = R$ 11.815,77`, mas `Valor Base total = R$ 12.056,91`, e este é o valor correto da nota.
- Marcelo salvou a credencial da BANESTES em `~/.openclaw/secrets/banestes.env`.
- A BANESTES não existia ainda como tomador cadastrado no emissor da Virei Contador, então foi criado o cadastro fiscal `BANESTES SEGUROS SA` com `clientId 1890`.
- Foi emitida a NF da BANESTES para competência `03/2026` usando o valor validado de `R$ 12.056,91`.
- Dados da emissão BANESTES: venda `3030`, NFSe `60`, status final `EMITIDA`, `spedyNfsId` `64d0b20a-d236-4fc1-a20a-b7f8d966efbf`, PDF em `state/nf-teste/NFSE-BANSEG-60.pdf`.
- A linha da BANESTES em `lancamentos_mensais!A6:P6` foi atualizada com descrição `COMISSÕES SOBRE VENDAS COMPETÊNCIA 03/2026`, número da nota `60` e status `EMITIDA`.
- Marcelo definiu uma regra importante para os próximos casos: usar a NF de referência no Drive como parâmetro antes de emitir, principalmente para tomador, CNPJ, descrição e demais campos fiscais.
- Marcelo enviou o mapeamento inicial da ALLIANZ.
- A ALLIANZ ficou consolidada com portal `https://www.allianznet.com.br/ngx-epac/private/application`, tela `Gestão > Financeiro > Extrato de Comissões`, filtro por período completo do mês anterior e uso do bloco `Comissões Pagas`.
- O print da ALLIANZ validou que o campo correto para `valor_nota` é `Total Bruto`, não `Total Líquido`.
- Exemplo validado por print: período `01/03/2026` a `31/03/2026`, `Total Bruto = R$ 8.584,31`.
- Marcelo salvou a credencial da ALLIANZ em `~/.openclaw/secrets/allianz.env`.
- A NF de referência da ALLIANZ no Drive foi usada como parâmetro fiscal, confirmando tomador `ALLIANZ SEGUROS S/A`, CNPJ `61.573.796/0029-67`, descrição base `SERVIÇOS DE CORRETAGEM COMPETÊNCIA 12/2025`, código nacional `10.01.02` e ISS retido pelo tomador.
- Foi emitida a NF da ALLIANZ para competência `03/2026` com valor `R$ 8.584,31`.
- Dados da emissão ALLIANZ: venda `3031`, NFSe `61`, status final `EMITIDA`, `spedyNfsId` `6bd3bc00-7677-4caf-9b1d-353b4d895332`, PDF em `state/nf-teste/NFSE-ALLIANZ SEGUROS S-A-61.pdf`.
- A linha da ALLIANZ em `lancamentos_mensais!A4:P4` foi atualizada com descrição `SERVIÇOS DE CORRETAGEM COMPETÊNCIA 03/2026`, número da nota `61` e status `EMITIDA`.
- Marcelo enviou o mapeamento inicial da HDI.
- A HDI ficou consolidada com portal `https://www.hdi.com.br/digital2/home`, tela `Adm > Relatórios > Comissões`, filtro por período completo do mês anterior e ação `Visualizar`.
- O print da HDI validou que o campo correto para `valor_nota` é o total de `Vlr. Bruto`, não `Líquida`.
- Exemplo validado por print: período de março/2026 com total `Vlr. Bruto = R$ 5.161,81`.
- Marcelo salvou a credencial da HDI em `~/.openclaw/secrets/hdi.env`.
- A NF de referência da HDI no Drive foi usada como parâmetro fiscal, confirmando tomador `HDI SEGUROS S.A.`, CNPJ `29.980.158/0058-92`, descrição base `SERVIÇOS DE CORRETAGEM COMPETÊNCIA 12/2025`, código nacional `10.01.02` e ISS retido pelo tomador.
- A HDI não existia ainda como tomador cadastrado no emissor da Virei Contador, então foi criado o cadastro fiscal `HDI SEGUROS S.A.` com `clientId 1891`.
- Foi emitida a NF da HDI para competência `03/2026` com valor `R$ 5.161,81`.
- Dados da emissão HDI: venda `3032`, NFSe `62`, status final `EMITIDA`, `spedyNfsId` `81ddada5-5a65-4666-97a8-632e988c7268`, PDF em `state/nf-teste/NFSE-HDI SEGUROS S.A.-62.pdf`.
- A linha da HDI em `lancamentos_mensais!A8:P8` foi atualizada com descrição `SERVIÇOS DE CORRETAGEM COMPETÊNCIA 03/2026`, número da nota `62` e status `EMITIDA`.
- Marcelo enviou o mapeamento inicial da BRADESCO SEGUROS.
- A BRADESCO SEGUROS ficou consolidada com portal `Auto > Comissões > Extrato de Comissões`, filtro por período completo do mês anterior, empresa `Bradesco Auto Re`, agência/conta `Todas` e ação `Buscar`.
- O print da BRADESCO SEGUROS validou que o campo correto para `valor_nota` é o card `Comissões`, não `Crédito líquido`.
- Exemplo validado por print: período de março/2026 com `Comissões = R$ 2.274,67`, `Tributações = R$ 44,36` e `Crédito líquido = R$ 2.173,90`.
- Ao validar o arquivo `~/.openclaw/secrets/bradesco_seguros.env`, apareceu um erro de parametrização: a chave `BRADESCO_SEGUROS_LOGIN` foi gravada duas vezes e a chave `BRADESCO_SEGUROS_PASSWORD` não está presente.
