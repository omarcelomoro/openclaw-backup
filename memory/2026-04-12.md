# 2026-04-12

## Memórias duráveis

- O nome final da assistente foi confirmado por Marcelo como **Livri**. `Smart` deve ser tratado apenas como nome histórico.
- O cérebro da Leevre foi estruturado e sincronizado com o repositório GitHub `omarcelomoro/openclaw-backup` via SSH.
- O GitHub do servidor foi autenticado por chave SSH (`~/.ssh/id_ed25519`) e o workspace foi ligado ao remote `git@github.com:omarcelomoro/openclaw-backup.git`.
- Houve merge entre o histórico local e o remoto. Foram preservadas as versões locais mais recentes de arquivos centrais do cérebro (`SOUL.md`, `USER.md`, `IDENTITY.md`, `AGENTS.md`, `MEMORY.md`).
- O backup diário local foi implementado em `/home/marcelo/.openclaw/workspace/state/backup-workspace.sh` e agendado via `crontab` para rodar diariamente às 03:00 UTC.
- O script de backup agora cria `.tar.gz` local, faz `git add -A`, cria commit automático quando houver mudanças e faz push para o GitHub via SSH.
- Foi testado com sucesso que o push automático para o GitHub funciona.
- O repositório GitHub agora contém a base estruturada do cérebro da Leevre, incluindo:
  - `empresa/`
  - `areas/operacoes/`
  - `areas/vendas/`
  - `areas/marketing/`
  - `areas/atendimento/`
  - `agentes/`
  - `rotinas/`
  - `skills/`
  - memória estruturada em `memory/`
- Foram criados e enviados ao GitHub materiais duráveis da Leevre, incluindo:
  - mapa do cérebro
  - visão e metas
  - métricas principais
  - pessoas-chave
  - decisões estratégicas
  - pipeline de consórcio
  - propriedades recomendadas do HubSpot
  - plano de 90 dias
  - rotinas operacionais e comerciais
  - pilares de conteúdo e tracking mínimo
  - FAQ inicial, triagem e handoff
  - mapa e identidade funcional dos agentes
  - primeiras skills autorais da Leevre
- As primeiras skills autorais criadas para a Leevre foram:
  - `auditoria-hubspot-leevre`
  - `gestao-renovacoes-seguro`
  - `followup-consorcio`
  - `analise-concorrentes-conteudo`
  - `consolidacao-metricas-semanais`
- O acesso ao Google Drive via `gog` segue pendente por falta do arquivo OAuth client JSON no servidor. Quando isso for resolvido, a próxima etapa é ler e adaptar o material compartilhado por Marcelo no Drive.
- Marcelo pediu explicitamente que a assistente seja mais proativa e não apenas descreva próximos passos. Essa diretriz deve continuar orientando a execução.
# 2026-04-12

## Memórias duráveis

- O nome final da assistente foi confirmado por Marcelo como **Livri**. `Smart` deve ser tratado apenas como nome histórico.
- O cérebro da Leevre foi estruturado e sincronizado com o repositório GitHub `omarcelomoro/openclaw-backup` via SSH.
- O GitHub do servidor foi autenticado por chave SSH (`~/.ssh/id_ed25519`) e o workspace foi ligado ao remote `git@github.com:omarcelomoro/openclaw-backup.git`.
- Houve merge entre o histórico local e o remoto. Foram preservadas as versões locais mais recentes de arquivos centrais do cérebro (`SOUL.md`, `USER.md`, `IDENTITY.md`, `AGENTS.md`, `MEMORY.md`).
- O backup diário local foi implementado em `/home/marcelo/.openclaw/workspace/state/backup-workspace.sh` e agendado via `crontab` para rodar diariamente às 03:00 UTC.
- O script de backup agora cria `.tar.gz` local, faz `git add -A`, cria commit automático quando houver mudanças e faz push para o GitHub via SSH.
- Foi testado com sucesso que o push automático para o GitHub funciona.
- O repositório GitHub agora contém a base estruturada do cérebro da Leevre, incluindo:
  - `empresa/`
  - `areas/operacoes/`
  - `areas/vendas/`
  - `areas/marketing/`
  - `areas/atendimento/`
  - `agentes/`
  - `rotinas/`
  - `skills/`
  - memória estruturada em `memory/`
- Foram criados e enviados ao GitHub materiais duráveis da Leevre, incluindo:
  - mapa do cérebro
  - visão e metas
  - métricas principais
  - pessoas-chave
  - decisões estratégicas
  - pipeline de consórcio
  - propriedades recomendadas do HubSpot
  - plano de 90 dias
  - rotinas operacionais e comerciais
  - pilares de conteúdo e tracking mínimo
  - FAQ inicial, triagem e handoff
  - mapa e identidade funcional dos agentes
  - primeiras skills autorais da Leevre
- As primeiras skills autorais criadas para a Leevre foram:
  - `auditoria-hubspot-leevre`
  - `gestao-renovacoes-seguro`
  - `followup-consorcio`
  - `analise-concorrentes-conteudo`
  - `consolidacao-metricas-semanais`
- O acesso ao Google Drive via `gog` segue pendente por falta do arquivo OAuth client JSON no servidor. Quando isso for resolvido, a próxima etapa é ler e adaptar o material compartilhado por Marcelo no Drive.
- Marcelo pediu explicitamente que a assistente seja mais proativa e não apenas descreva próximos passos. Essa diretriz deve continuar orientando a execução.
- O OAuth do `gog` para `atendimento@leevrecorretora.com.br` foi concluído com sucesso usando fluxo remoto/manual (`--remote --step 1/2`) após upload do `credentials.json` via `scp` do Windows para `/home/marcelo/.config/gogcli/credentials.json`.
- Foi necessário resetar o keyring antigo do `gog` (`~/.config/gogcli/keyring`) porque a senha anterior não estava disponível; Marcelo criou uma nova senha localmente via `GOG_KEYRING_PASSWORD`.
- As APIs Google Drive, Docs e Sheets foram ativadas no projeto `905139521903` para fazer o `gog` funcionar.
- O primeiro ID de pasta testado no Drive (`1joXq6DLEIuNdTyhvziaAsCQT9QINoLnD`) retornou vazio/404 e deve ser tratado como ID incorreto ou não compartilhado com a conta `atendimento@leevrecorretora.com.br`.
- Marcelo enviou um novo link de pasta do Google Drive com ID `1quUQlgQrfvyk4hENDVTUoHwEVOw_Nm0M`, que passa a ser o candidato atual para testar leitura e importação dos materiais compartilhados.
