
## Crabbox v0.3.0 instalado
- Auditada release https://github.com/openclaw/crabbox/releases/tag/v0.3.0 em 2026-05-02.
- Binário Linux amd64 instalado em `/root/.local/bin/crabbox`; checksum do arquivo `crabbox_0.3.0_linux_amd64.tar.gz` validado: `11191030661c5f40ffb01205b312ca22efda73dc6064b68fb9e1904899867567`.
- Plugin OpenClaw `crabbox` instalado a partir do source tag `v0.3.0` em `~/.openclaw/extensions/crabbox`, habilitado e Gateway reiniciado.
- Scanner bloqueou inicialmente por uso de `child_process`; override aplicado porque o plugin é um wrapper esperado que chama o binário `crabbox`.
- Validação: `crabbox --version` = 0.3.0; `openclaw plugins doctor` sem issues; ferramentas registradas: `crabbox_run`, `crabbox_warmup`, `crabbox_status`, `crabbox_list`, `crabbox_stop`.
- `crabbox doctor` ok para git/ssh/ssh-keygen/rsync/curl; pendência: `missing hcloud token` se for usar Hetzner direto/broker que dependa disso.
